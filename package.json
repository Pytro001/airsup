{
  "private": true,
  "workspaces": ["server"],
  "scripts": {
    "build": "cd server && npm install && npx tsc",
    "dev": "cd server && npm run dev"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0"
  }
}
